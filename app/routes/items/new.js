import Ember from 'ember';

export default Ember.Route.extend({
  // model() {
  //   var location_id = "8R4YX63ESVXRE";
  //   var endpoint = "https://connect.squareup.com/v1/" + location_id + "/items";

  //   var items = function httpGet(endpoint) {
  //   var xmlHttp = new XMLHttpRequest();
  //   xmlHttp.open( "POST", endpoint, false ); // false for synchronous request
  //   xmlHttp.setRequestHeader("ITEMS_WRITE", ENV.APP.SQUARE_PERSONAL_ACCESS_TOKEN);
  //   xmlHttp.setRequestHeader("Accept", "application/json");
  //   xmlHttp.send( null );
  //   return xmlHttp.responseText;
  // };
  //   return JSON.parse(items(endpoint));
  // }
});
